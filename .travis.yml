language: cpp
dist: trusty

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y python python-dev

before_script:
    - mkdir build
    - cd build
    - cmake ..

script:
    - make -j8 all
    - make test
